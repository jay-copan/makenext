# this script is going to ensure that nextjs is installed with default settings

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/../scripts/colors.sh"

echo -e "\n${BLUE}➜ Creating Next.js app${RESET}"
npx create-next-app@latest . --yes
